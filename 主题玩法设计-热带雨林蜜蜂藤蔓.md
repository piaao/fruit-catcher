# 🥭 热带雨林 · 主题四玩法设计

> 第31-40关 | 角色：热带猎人 | 主题色：翠绿热带色 | 难度：高级

---

## 一、核心机制：蜜蜂追击 + 藤蔓障碍

热带雨林有两套相互配合的机制，难度较高，适合专家级别挑战。

---

### 机制 A：蜜蜂追击（Bee Swarm）

#### 设计理念

蜜蜂是雨林中的哨兵，会从屏幕边缘飞出，追击水果将其蜇伤（改变水果飞行方向，使其偏离角色）。玩家需要在蜜蜂蜇到水果前抢先吃掉，或利用蜜蜂将水果"赶"向角色。

#### 视觉效果

- **蜜蜂外观**：黄色小圆点 + 翅膀抖动动画，飞行时留下淡黄色轨迹
- **追击特效**：蜜蜂接近水果时，水果周围出现金色警告光环
- **蜇伤特效**：水果被蜇后轨迹改变，产生橙黄色粒子爆裂
- **背景粒子**：树叶（`leaf`）+ 偶尔飘过的花瓣

#### 机制参数

```
蜜蜂速度：比水果快约 1.3 倍
追击目标：随机锁定一个在场水果
蜇伤效果：水果速度方向随机偏转 30-60 度
持续时间：蜜蜂蜇完一次后飞离屏幕消失
出现频率：每 3-5 秒出现 1-3 只蜜蜂（随关卡难度增加）
```

#### 实现方案

```javascript
// ---- 蜜蜂系统（热带雨林主题）----
let beeState = {
  active: false,
  bees: [],         // [{x, y, vx, vy, target, state, timer}]
  strength: 0,      // 1=少量, 2=中量, 3=密集
};

// 每关初始化
function initBeesLevel() {
  const lvl = LEVELS[level];
  if (!lvl.beeStrength) { beeState.active = false; return; }
  beeState.active = true;
  beeState.strength = lvl.beeStrength;
}

// 每帧更新蜜蜂
function updateBees(dt) {
  if (!beeState.active) return;

  // 生成新蜜蜂
  const spawnRate = beeState.strength === 1 ? 0.3 : beeState.strength === 2 ? 0.4 : 0.5;
  if (Math.random() < spawnRate * dt && beeState.bees.length < beeState.strength * 2) {
    spawnBee();
  }

  beeState.bees.forEach(bee => {
    // 寻找最近的水果
    let closest = null, closestDist = Infinity;
    projectiles.forEach(p => {
      if (p.eaten || p.isBomb) return;
      const d = dist(bee.x, bee.y, p.x, p.y);
      if (d < closestDist) { closestDist = d; closest = p; }
    });

    if (closest) {
      // 追击方向
      const dx = closest.x - bee.x;
      const dy = closest.y - bee.y;
      const len = Math.hypot(dx, dy);
      const beeSpeed = 250;
      bee.vx = (dx / len) * beeSpeed;
      bee.vy = (dy / len) * beeSpeed;

      // 蜜蜂蜇到水果
      if (closestDist < closest.def.radius + 8) {
        // 偏转水果方向 30-60 度
        const deflect = (0.5 + Math.random()) * Math.PI / 3; // 30-60度
        const curAngle = Math.atan2(closest.vy, closest.vx);
        const newAngle = curAngle + deflect * (Math.random() < 0.5 ? 1 : -1);
        const cv = Math.hypot(closest.vx, closest.vy);
        closest.vx = Math.cos(newAngle) * cv;
        closest.vy = Math.sin(newAngle) * cv;
        // 效果
        spawnParticles(closest.x, closest.y, '#ffaa00', 12);
        addFloatingText(closest.x, closest.y - 20, '🐝', '#ffd700');
        bee.state = 'flying_away';
        bee.timer = 1.0;
      }
    }

    bee.x += bee.vx * dt;
    bee.y += bee.vy * dt;
    bee.timer -= dt;

    // 飞离或超时移除
    if (bee.timer <= 0 || bee.x < -50 || bee.x > W + 50 || bee.y < -50 || bee.y > H + 50) {
      bee.state = 'done';
    }
  });

  beeState.bees = beeState.bees.filter(b => b.state !== 'done');
}

function spawnBee() {
  const side = Math.floor(Math.random() * 4);
  let sx, sy;
  if (side === 0) { sx = Math.random() * W; sy = -20; }
  else if (side === 1) { sx = W + 20; sy = Math.random() * H; }
  else if (side === 2) { sx = Math.random() * W; sy = H + 20; }
  else { sx = -20; sy = Math.random() * H; }

  beeState.bees.push({ x: sx, y: sy, vx: 0, vy: 0, timer: 5, state: 'hunting' });
}
```

---

### 机制 B：藤蔓障碍（Vine Obstacles）

#### 设计理念

从屏幕顶部或侧面垂下藤蔓，形成半透明的动态障碍物。水果碰到藤蔓会被减速（摩擦力），增加玩家的反应时间。但藤蔓也会遮挡视线。

#### 视觉效果

- **藤蔓外观**：深绿色弯曲绳索状，从屏幕边缘垂下
- **动态效果**：藤蔓缓慢摇摆（sinusoidal 运动）
- **遮挡效果**：半透明，深色玩家可能部分不可见
- **摩擦特效**：水果接触藤蔓时产生绿色粒子

#### 实现方案

```javascript
// ---- 藤蔓系统（热带雨林主题）----
let vineState = {
  active: false,
  vines: [],    // [{x1, y1, x2, y2, swayPhase, swayAmp, swayFreq, length}]
  strength: 0,
};

function initVinesLevel() {
  const lvl = LEVELS[level];
  if (!lvl.vineStrength) { vineState.active = false; return; }
  vineState.active = true;
  vineState.strength = lvl.vineStrength;

  // 生成藤蔓
  const count = lvl.vineStrength;
  for (let i = 0; i < count; i++) {
    const fromTop = Math.random() < 0.5;
    vineState.vines.push({
      x1: 100 + Math.random() * (W - 200),
      y1: fromTop ? -20 : H + 20,
      x2: 0, y2: 0,
      length: 150 + Math.random() * 200,
      swayPhase: Math.random() * Math.PI * 2,
      swayAmp: 20 + Math.random() * 30,
      swayFreq: 0.5 + Math.random() * 0.5,
      swayAngle: 0,
    });
  }
}

function updateVines(dt) {
  if (!vineState.active) return;
  vineState.vines.forEach(v => {
    v.swayPhase += dt * v.swayFreq;
    v.swayAngle = Math.sin(v.swayPhase) * v.swayAmp * 0.02;

    // 更新藤蔓端点（简化为旋转锚点）
    const fromTop = v.y1 < 0;
    const baseX = v.x1;
    const baseY = fromTop ? 0 : H;
    v.x2 = baseX + Math.sin(v.swayAngle) * v.length * 0.5;
    v.y2 = fromTop ? v.length : H - v.length;
  });
}

// 水果碰撞藤蔓检测（点到线段距离）
function checkVineCollision(p) {
  if (!vineState.active) return false;
  for (const v of vineState.vines) {
    const d = pointToSegmentDist(p.x, p.y, v.x1, v.y1, v.x2, v.y2);
    if (d < p.def.radius + 5) {
      // 摩擦减速
      p.vx *= 0.98;
      p.vy *= 0.98;
      spawnParticles(p.x, p.y, '#228b22', 5);
      return true;
    }
  }
  return false;
}
```

---

## 二、BGM 音乐风格

**风格**：热带活力，节奏感强，加勒比冒险感

- BGM-1: A小调热带曲，钢鼓（steel drum）+ 康加鼓（conga），加勒比风
- BGM-2: D小调丛林曲，木管 + 轻打击乐，探险感
- BGM-3: E大调阳光曲，尤克里里 + 口哨，轻松愉快
- BGM-4: C小调神秘曲，Marimba + 环境音（蛙鸣+鸟叫），古老遗迹感
- BGM-5: G大调庆典曲，全套打击乐 + 铜管，热情洋溢

---

## 三、炸弹外观变化

热带雨林的炸弹从 💣 变为 **火山弹** 🌋：
- 外观：小型火山锥，顶部冒烟
- 爆炸特效：橙红色熔岩粒子四散

---

## 四、难度曲线

```
baseSpeed:     224 → 260   (关卡31-40)
spawnInterval: 990 → 810   ms
bombChance:    0.170 → 0.210
target:        600 → 830
```

---

## 五、10关详细设计

| 关 | 关名 | target | baseSpeed | spawnInterval | bombChance | 蜜蜂强度 | 藤蔓数量 | 特殊说明 |
|----|------|--------|-----------|---------------|------------|---------|---------|---------|
| 31 | 雨林边缘 | 600 | 224 | 990 | 0.170 | 1级 | 1根 | 新主题过渡，引入蜜蜂 |
| 32 | 香蕉丛林 | 620 | 228 | 970 | 0.175 | 1级 | 1根 | 香蕉水果登场 |
| 33 | 芒果海岸 | 645 | 232 | 950 | 0.180 | 1级 | 2根 | 芒果水果登场 |
| 34 | 菠萝要塞 | 670 | 236 | 930 | 0.185 | 2级 | 2根 | 引入藤蔓，菠萝登场 |
| 35 | 瀑布秘境 | 695 | 240 | 910 | 0.190 | 2级 | 2根 | 小Boss关，密度增大 |
| 36 | 榴莲丛林 | 720 | 244 | 890 | 0.195 | 2级 | 3根 | 榴莲登场（高分高风险） |
| 37 | 火山脚下 | 745 | 248 | 870 | 0.200 | 2级 | 3根 | 火龙果登场 |
| 38 | 荔枝沼泽 | 770 | 252 | 850 | 0.205 | 3级 | 3根 | 荔枝/龙眼登场 |
| 39 | 椰子海滩 | 800 | 256 | 830 | 0.208 | 3级 | 4根 | 椰子登场（巨型障碍） |
| 40 | 热带之王 | 830 | 260 | 810 | 0.210 | 3级 | 4根 | 主题Boss关，全水果池 |

---

*热带雨林 · 玩法设计 v1.0 | 2026-04-21*
